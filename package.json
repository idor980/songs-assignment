{
  "name": "songs-assignment",
  "version": "1.0.0",
  "description": "A full-stack Song Collection Manager with React frontend, NestJS backend, and PostgreSQL database",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "dev": "docker-compose up -d && echo '⏳ Waiting for database to be ready...' && sleep 3 && cd backend && dotenv -e ../.env -- npm run prisma:generate && dotenv -e ../.env -- npm run prisma:migrate && dotenv -e ../.env -- npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && dotenv -e ../.env -- npm run start:dev",
    "db:start": "docker-compose up -d",
    "db:stop": "docker-compose down",
    "format": "cd backend && npm run format",
    "lint": "cd backend && npm run lint"
  },
  "keywords": [
    "nestjs",
    "react",
    "typescript",
    "postgresql",
    "prisma",
    "tailwind",
    "flowbite",
    "songs",
    "csv",
    "docker"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  }
}
